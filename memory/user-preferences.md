---
name: user-preferences
description: Dann's preferences for working on chris-auto-shine — auto-commit permitted on Dannflow branch
metadata:
  type: feedback
---

Auto-commit at each meaningful stage is permitted on the `Dannflow` branch.

**Why:** User explicitly said "i permit you to auto commit message in every stage" when doing the Next.js restructure.

**How to apply:** When working on the Dannflow branch, commit after each logical stage without asking first. Use descriptive commit messages with stage labels. Still ask before pushing to remote.
